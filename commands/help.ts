import { Params, Options } from "../types.ts";
export default async function help(params: Params, options: Options) {
  console.log("Not yet implemented");
  Deno.exit(1);
  if (params.length === 0) {

  }
}
